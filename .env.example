# WordPress GraphQL endpoint the Soames Astro front end reads at dev/build time.
# Copy this file to `.env` and set your site's WPGraphQL URL (use https in production).
WORDPRESS_GRAPHQL_URL=https://your-site.example.com/graphql
